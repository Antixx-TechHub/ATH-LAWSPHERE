# Railway configuration
# Using Nixpacks for more reliable builds

[build]
builder = "nixpacks"
watchPatterns = ["apps/web/**"]

[deploy]
healthcheckPath = "/_health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
