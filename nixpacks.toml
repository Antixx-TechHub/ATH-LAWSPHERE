# Nixpacks configuration for Railway
# Force fresh build: 2026-01-06-v5

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci --workspaces --include-workspace-root || npm install --workspaces --include-workspace-root"]

[phases.build]
cmds = [
    "rm -rf apps/web/.next/cache/* node_modules/.cache/* 2>/dev/null || true",
    "cd apps/web && npx prisma generate",
    "cd apps/web && npm run build"
]

[start]
cmd = "cd apps/web && npx prisma migrate deploy && node server.js"

[variables]
NODE_ENV = "production"
