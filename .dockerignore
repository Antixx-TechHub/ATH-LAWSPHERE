# Docker build context ignores - MINIMAL to avoid issues

# Git folder (not needed, large)
.git

# IDE
.idea
.vscode

# Local development
.env.local
.env.development.local

# Build outputs (we rebuild in Docker)
apps/web/.next
apps/web/out

# Docs folder (not needed for build)
docs
Artifacts

# Test samples (not needed for build)  
test-samples
